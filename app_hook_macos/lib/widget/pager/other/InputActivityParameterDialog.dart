import 'package:flutter/material.dart';

class InputActivityParameterDialog extends Dialog {
  String data;

  InputActivityParameterDialog({Key key, this.data = ""}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return super.build(context);
  }
}
